name = "places-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2024-12-01"

[build]
command = "cargo install worker-build && worker-build --release"

# Secret GOOGLE_MAPS_API_KEY set via: npx wrangler secret put GOOGLE_MAPS_API_KEY
